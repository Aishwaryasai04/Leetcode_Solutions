class Solution(object):
    def isAnagram(self, s, t):
        """
        :type s: str
        :type t: str
        :rtype: bool
        """
        r=s.split()
        for i in range(r):
            if r.char==t:
                return true
